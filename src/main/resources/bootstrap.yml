spring:
  application:
    name: picpay-challenge
  profiles:
    active: default
  cloud:
    config:
      discovery:
        enabled: true
        service-id: picpay-challenge-config-server

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka